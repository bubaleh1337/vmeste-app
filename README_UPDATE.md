# Update 5.5 — faster saving from the goals page

- Приложение переименовано в «Копим вместе» / Saving Together.
- Карточки целей на главной показывают накопленную сумму, процент и полосу прогресса.
- На каждой активной карточке есть заметная кнопка «Добавить накопление», которая сразу открывает ручную форму.
- Внутри цели ручное пополнение тоже оформлено как полноценная кнопка.
- Подтверждение отрицательной корректировки показывается только когда введённая сумма действительно уведёт баланс выбранной валюты ниже нуля.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 5.4 — localized product name

- В русской версии название приложения отображается как «Вместе», в английской — Together.
- Локализованы интерфейс, страница входа, публичная и правовые страницы, донатные тексты, заголовок вкладки и PWA-манифест.
- Русское и английское названия при необходимости переопределяются отдельно через `NEXT_PUBLIC_APP_NAME` и `NEXT_PUBLIC_APP_NAME_EN`.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 5.3 — iPhone session and PDF reliability

- Установленное на iPhone веб-приложение восстанавливает сохранённую Supabase-сессию после запуска, возврата из фона и восстановления интернета.
- Возобновление PWA больше не зависит только от фоновых таймеров, которые iOS может заморозить.
- PDF-импорт получил совместимость с Safari для `Promise.withResolvers` и асинхронного чтения `ReadableStream`.
- Выписка Halyk по продуктовой карте распознаётся как текстовая и даёт пять расходных операций; входящий перевод не попадает в расходы.
- Исходный PDF по-прежнему обрабатывается локально в браузере и не загружается на сервер.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 5.2 — voluntary project support

- На публичной главной странице и в разделе помощи появилась добровольная поддержка проекта через Buy Me a Coffee.
- Оплата открывается в новой вкладке на официальной странице автора: `https://buymeacoffee.com/kate.asmdef`.
- «Вместе» не получает данные карты, не хранит платёжные сведения и не проверяет факт поддержки.
- Кнопка доступна на русском и английском языках и адаптирована для мобильных экранов.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 5.1 — manual entry confirmation

- При ручном добавлении накопления кнопка показывает «Добавляю…», а после сохранения — «Добавлено ✓».
- Под кнопкой появляется явное подтверждение, поэтому больше не нужно прокручивать страницу к общей сумме, чтобы понять, сработало ли действие.
- После успешного добавления поля очищаются, а повторное нажатие во время отправки заблокировано.
- Такое же поведение добавлено ручным расходам.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 5.0 — accessibility and security hardening

- Основные кнопки всех шести тем теперь проходят WCAG AA по контрасту.
- Клавиатурный focus стал единым и хорошо заметным на всех интерактивных элементах.
- Усилены безопасные HTTP-заголовки публичного приложения.
- Добавлен регрессионный тест контрастности тем.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 4.9 — app icon

- Добавлен знак из двух соединённых округлых форм — символ совместной цели.
- Знак используется в интерфейсе вместо буквенной плашки.
- Подключены favicon, Apple Touch Icon и PWA-иконки 192/512 px с maskable-вариантом.
- Миграции Supabase и новые npm-пакеты не требуются.

# Update 4.8 — duplicate protection

- Перекрывающиеся выписки можно загружать повторно: ранее сохранённые операции будут пропущены, новые — добавлены.
- Защита работает не только для Отбасы/Halyk: импорт остаётся bank-agnostic и применим к Kaspi, Freedom и другим банкам.
- Стабильный ID/Reference/RRN используется автоматически, когда он есть в CSV/XLSX или распознан в PDF.
- Без ID используется fingerprint с учётом количества одинаковых операций, поэтому две реальные одинаковые транзакции не склеиваются.
- Номер банковского счёта не сохраняется: используется только SHA-256 отпечаток, если счёт удалось определить.
- Требуется миграция `supabase/migrations/202609010004_duplicate_protection.sql`.

# Обновление 4.7

Версия приложения: `0.10.0`.

## Что изменилось

- В раскрытом блоке «Накопления» кнопки «Добавить накопление» и «Импортировать файл» находятся сразу под заголовком.
- В раскрытом блоке «Расходы» кнопки «Добавить расход» и «Импортировать файл» находятся сразу под заголовком.
- Списки операций больше не растягивают главный экран цели.
- «История пополнений» открывает отдельную страницу со всеми операциями накоплений.
- «История расходов» открывает отдельную страницу со всеми расходами.
- Истории отсортированы от новых операций к старым и сохраняют действия редактирования/удаления.

SQL-миграции и новые npm-пакеты не требуются.
